msdev:
	msdev REG.dsp /make "REG - Win32 Release"

msbuild.x64:
	msbuild REG.sln /p:Configuration=Release /p:Platform=x64

msbuild.x86:
	msbuild REG.sln /p:Configuration=Release /p:Platform=Win32

clean:
	-del /s /f /q *.lib *.obj
