msdev:
	msdev CMD.dsp /make "CMD - Win32 Release"

msbuild.x64:
	msbuild CMD.sln /p:Configuration=Release /p:Platform=x64

msbuild.x86:
	msbuild CMD.sln /p:Configuration=Release /p:Platform=Win32

clean:
	-del /s /f /q *.lib *.obj
