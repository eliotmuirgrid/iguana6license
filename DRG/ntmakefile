msdev:
	msdev DRG.dsp /make "DRG - Win32 Release"

msbuild.x64:
	msbuild DRG.sln /p:Configuration=Release /p:Platform=x64

msbuild.x86:
	msbuild DRG.sln /p:Configuration=Release /p:Platform=Win32

clean:
	-del /s /f /q *.lib *.obj
